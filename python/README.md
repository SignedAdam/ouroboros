# Ouroboros — Python

⏳ **Planned.** A working reference implementation already exists in Vaux at
`~/dev/vaux/vaux/ouroboros/` (units: `issue.py`, `store.py`, `agent.py`, `terminal.py`,
`facade.py`, with tests under `tests/test_ouroboros_*.py`). Port it here as the canonical,
vaux-free version and add an `INTEGRATION.md` mirroring `swift/INTEGRATION.md`.
